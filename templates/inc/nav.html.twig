<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <a class="navbar-brand" href="{{ url('main') }}">PGX F1 Telemetry</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ url('main') }}">Home</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ url('download') }}">Download</a>
            </li>

            <li class="nav-item active">
                <a class="nav-link" href="{{ url('support') }}">Support</a>
            </li>
        </ul>
        {% if app.user %}


            <span class="navbar-text">Hello {{ app.user.pseudo }}
                    {% for role in app.user.roles %}
                        {% if role == 'ROLE_ADMIN' %}
                            (admin)
                        {% endif %}
                    {% endfor %}

                    </span>

            <ul class="nav navbar-nav">


                <li class="nav-item active">
                    <a class="nav-link" href="{{ url('admin_index') }}">Administration</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_logout') }}">Log out</a>
                </li>

            </ul>
        {% endif %}
    </div>
</nav>

</nav>
